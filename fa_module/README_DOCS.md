FA Sanity Check — Documentation

Generating API docs (phpDocumentor):

1. Install phpDocumentor globally or as a dev dependency.
2. From `fa_module/` run:

```bash
phpdoc -c phpdoc.xml
```

Output will be written to `fa_module/docs/api`.
